import java.util.ArrayList;
import java.util.LinkedList;

/**
 * Given a binary tree, design an algorithm which creates a linked list of all the nodes at each depth (eg: if you have a tree of depth D, you'll have D linked lists)
 */


public class ListOfDepthsOfBinaryTree {

    ArrayList<LinkedList<TreeNode>> listDepths(TreeNode treeNode) {
        if (treeNode == null) {
            return null;
        }

        return null;
    }

}
